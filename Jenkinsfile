pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Clarusway Way to Reinvent Yourself'
                sh 'python3 --version'
                sh 'python3 pipeline.py'
            }
        }
    }
}